div(ng-controller="FilmsController as ctrl")
    div(class="row")
        div(class="col-lg-12")
            h3 Introduce some movie title here
            form(class="search-form")
                div(class="form-group")
                    input(class="form-control" ng-model="title" type="text" placeholder="Search")
                    button(class="btn btn-default" ng-click="ctrl.getFilmsByTitle(title)" type="submit") Find It!
    
        div(class="col-lg-3 col-md-6" ng-repeat="film in ctrl.films")
            div(class="thumbnail")
                img(src="{{film.poster}}" alt="{{film.title}}")
                div(class="caption")
                    h3 {{film.title}}
                    p Lorem ipsum dolor sit amet, consectetur adipiscing elit. 
                    p
                        a(class="btn btn-primary" ui-sref="films.detail({id:film.imdbID})") Details
                        a(class="btn btn-default" ng-click="ctrl.saveLike(film)" ng-if="user && !ctrl.isAlreadyFavourite(film.imdbID)") I like it
        hr
