export default class User {

        constructor(username, password, favourites) {
                this.username = username;
                this.password = password;
                this.favourites = favourites;
        }

}